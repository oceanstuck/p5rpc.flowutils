# Flowscript Utilities

A small mod with a couple general-use flow functions. To use, copy the Functions.json into your mod\'s FEmulator/BF folder, or add the functions you need to your own Functions.json if you already have one, then call the functions in your own flowscript hooks.